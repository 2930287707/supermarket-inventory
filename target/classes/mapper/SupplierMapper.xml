<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.supermarketinventory.mapper.SupplierMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supplier (name, contact, phone, address, create_time)
        VALUES (#{name}, #{contact}, #{phone}, #{address}, NOW())
    </insert>

    <update id="update">
        UPDATE supplier
        SET name = #{name}, contact = #{contact}, phone = #{phone}, address = #{address}
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM supplier WHERE id = #{id}
    </delete>

    <select id="findAll" resultType="com.supermarket.supermarketinventory.entity.Supplier">
        SELECT * FROM supplier ORDER BY create_time DESC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM supplier
    </select>
</mapper>
